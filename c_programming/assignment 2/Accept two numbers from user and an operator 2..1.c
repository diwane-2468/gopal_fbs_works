#include <stdio.h>

int main() {
    int num1, num2;
    char op;

    // Input two numbers
    printf("Enter two numbers: ");
    scanf("%d %d", &num1, &num2);

    // Input operator
    printf("Enter an operator (+, -, *, /, %%): ");
    scanf(" %c", &op); 
    
        case '+':
            printf("Result = %d\n", num1 + num2);
            brea
